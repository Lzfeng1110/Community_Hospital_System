# 大观街道社区卫生服务中心智能问答系统 · 部署说明

## 一、环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 或 Linux/macOS |
| Python  | 3.10 及以上 |
| 内存    | 建议 4GB 以上（嵌入模型约占 500MB）|
| 磁盘    | 建议 5GB 以上（含 Python 环境） |
| 网络    | 需访问 DeepSeek API（api.deepseek.com）|

---

## 二、快速部署（Windows）

### 第一步：获取 DeepSeek API Key
1. 访问 [https://platform.deepseek.com](https://platform.deepseek.com) 注册账号
2. 在控制台创建 API Key
3. 充值（按 token 计费，费用极低）

### 第二步：配置环境变量
```
复制 .env.example → .env
编辑 .env，填写：
  DEEPSEEK_API_KEY=sk-xxxxxxxxxxxx
  ADMIN_PASSWORD=your_strong_password
```

### 第三步：一键启动
```
双击运行 start.bat
```

首次启动会自动：
- 创建 Python 虚拟环境
- 安装所有依赖包
- 启动 Streamlit 服务

浏览器访问：http://localhost:8501

---

## 三、手动部署（命令行）

```bash
# 1. 创建虚拟环境
python -m venv venv

# Windows 激活
venv\Scripts\activate

# 2. 安装依赖
pip install -r requirements.txt

# 3. 配置环境变量
copy .env.example .env
# 编辑 .env 填写 API Key

# 4. 启动应用
streamlit run frontend/app.py --server.port 8501
```

---

## 四、首次使用流程

### 1. 上传知识文档（必须先做）
1. 点击侧边栏「🗂️ 后台管理」
2. 输入管理员密码（默认：admin@2024，建议修改）
3. 选择文件所属模块（医保政策 / 疫苗 / 预约 / 通知）
4. 上传 PDF / Word / TXT 文件
5. 点击「保存并重建向量库」

> 知识库文件建议：
> - policy/：医保政策文件、家庭医生签约协议
> - vaccine/：预防接种程序表、疫苗说明
> - appointment/：门诊时刻表、体检须知
> - notice/：社区公告、活动通知

### 2. 开始问答
- 选择对应模块
- 在输入框中用自然语言提问
- 或点击左侧快捷问题

---

## 五、项目结构说明

```
项目根目录/
├── frontend/
│   ├── app.py                  # Streamlit 主入口
│   ├── components/
│   │   └── sidebar.py          # 侧边栏组件
│   └── pages/
│       ├── chat_page.py        # 智能问答页
│       ├── admin_page.py       # 后台管理页
│       └── stats_page.py       # 统计分析页
├── backend/
│   ├── config.py               # 全局配置
│   ├── rag_engine.py           # RAG核心（LangChain+FAISS+DeepSeek）
│   ├── knowledge_manager.py    # 知识库文件管理
│   └── log_manager.py          # SQLite日志与统计
├── knowledge_base/             # 原始文档存储
│   ├── policy/                 # 医保/家庭医生政策
│   ├── vaccine/                # 疫苗接种文档
│   ├── appointment/            # 预约办事指南
│   └── notice/                 # 社区公告通知
├── vector_store/               # FAISS向量索引（自动生成）
├── logs/
│   └── query_logs.db           # SQLite问答日志
├── .streamlit/
│   └── config.toml             # Streamlit主题配置
├── requirements.txt
├── .env                        # 环境变量（不提交git）
├── .env.example                # 环境变量模板
└── start.bat                   # Windows一键启动脚本
```

---

## 六、功能说明

### 智能问答（居民端）
- 支持 4 个专业模块 + 全库检索
- 流式输出，实时显示回答
- 自动显示引用来源（文件名+页码）
- 聊天历史记忆（同一会话内）
- 涉及医疗敏感词自动添加免责声明
- 侧边栏快捷问题一键提问

### 后台管理（管理员端）
- 密码登录保护
- 多文件批量上传（PDF/Word/TXT）
- 按模块分类管理文档
- 一键重建/清空向量库
- 文件列表查看与删除

### 统计分析（管理员端）
- 每日咨询量趋势图
- 各模块咨询占比饼图
- 高频问题排行榜（支持按模块筛选）
- 最近50条咨询记录

---

## 七、技术架构

```
居民提问
    ↓
Streamlit 前端（流式渲染）
    ↓
RAG Engine (LangChain LCEL)
    ├── HuggingFace Embeddings (BAAI/bge-small-zh-v1.5)
    │       ↓ 向量检索
    │   FAISS 向量库（按模块分库）
    │       ↓ Top-K 相关文档
    └── DeepSeek Chat（流式生成）
            ↓ 含上下文历史 + 引用来源
    回答 + 引用来源 → 前端渲染
    ↓
SQLite 日志记录
```

---

## 八、常见问题

**Q：首次启动很慢？**
A：第一次会自动下载嵌入模型（~130MB），需要网络连接。之后启动会从缓存加载，速度较快。

**Q：上传文件后回答不准确？**
A：确保点击了「重建向量库」。文档质量越高（条理清晰、格式规范），回答越准确。

**Q：如何更换嵌入模型？**
A：修改 `.env` 中的 `EMBEDDING_MODEL`，或在 `backend/config.py` 中直接修改。注意更换模型后需重建所有向量库。

**Q：能同时支持多少用户？**
A：Streamlit 默认单实例，建议 10 人以下并发。如需更大并发，可部署多实例或改用 FastAPI + React。

**Q：数据安全性？**
A：所有数据存储在本地（知识文档、向量库、日志），仅问答请求会发送至 DeepSeek API。

---

## 九、生产环境建议

1. **修改管理员密码**：在 `.env` 中设置强密码
2. **反向代理**：使用 Nginx 代理，配置 HTTPS
3. **进程守护**：使用 PM2 或 systemd 保持服务运行
4. **定期备份**：备份 `knowledge_base/`、`vector_store/`、`logs/` 目录
5. **日志轮转**：SQLite 日志定期归档，防止文件过大

### Nginx 配置参考
```nginx
server {
    listen 80;
    server_name your-domain.com;
    location / {
        proxy_pass http://127.0.0.1:8501;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }
}
```
